name: release

on:
  push:
    tags:
      - 'version-*.*.*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Prepare
        run: build/make_release -t ${{ runner.temp }}
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          body_path: ${{ runner.temp }}/release_notes.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
